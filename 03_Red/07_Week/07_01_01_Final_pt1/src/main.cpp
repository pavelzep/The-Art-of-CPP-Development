#include "global.h"
#include "tests.h"

#include <iterator>

int main() {

    // string s = "q w e r t y";
    // istringstream is(s);
    // istream_iterator<string> it(is);
    // while (it != istream_iterator<string>()) {
    //     cout << *it++ << ' ';
    // }
    TestAll();
}
